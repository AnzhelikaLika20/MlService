# ML Manager — проект

## Быстрый старт (локально)
1. Скопировать `.env.example` -> `.env` и заполнить переменные.
2. Сборка и запуск (локально, docker):
   ```bash
   make build
   make up
3. Открыть http://localhost:8000/docs для Swagger.